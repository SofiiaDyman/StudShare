# Student Housing

**StudShare** — веб-платформа для допомоги студентам Львівського національного університету імені Івана Франка у пошуку житла та співмешканців.

 # Опис проекту
StudShare - це спеціалізована платформа де студенти можуть:

📋 Реєструватися  

🔍 Переглядати оголошення про вільне житло  

➕ Публікувати свої оголошення  

✏️ Редагувати та видаляти свої оголошення 

❤️ Зберігати улюблені варіанти  

🎯 Фільтрувати оголошення за ціною, районом, факультетом та статю  

📱 Зв'язуватися з власниками через телефон, Telegram або Instagram

---

## Технології та бібліотеки

- **Backend:** Node.js, Express.js
- **Frontend:** HTML, CSS, JavaScript (Vanilla)
- **База даних:** MySQL
- **Документація API:** Swagger
- **Авторизація/Автентифікація:** JWT, Cookies
- **Інші бібліотеки:**
  - `mysql2` — для роботи з MySQL
  - `bcryptjs` — для хешування паролів користувачів
  - `body-parser` — для парсингу JSON та form-data з запитів
  - `cookie-parser` — для роботи з cookies
  - `cors` — для налаштування політики CORS між фронтендом і бекендом
  - `express` — фреймворк для створення серверної частини та API
  - `jsonwebtoken` — для створення та перевірки JWT токенів
  - `swagger-jsdoc` — для генерації документації API зі Swagger
  - `swagger-ui-express` — для інтеграції Swagger
  - `dotenv` — для роботи з конфігураціями та зберігання секретів у файлі .env
- **Інструменти для розробки:**
 - `nodemon` — автоматичний перезапуск сервера при зміні файлів.

---

## Структура проекту

```
student-housing/
│
├── backend/
│   ├── db.js                // Підключення до бази даних
│   ├── server.js            // Запуск сервера Express
│   ├── swagger.js           // Swagger API документація
│   ├── package.json         // Залежності та скрипти
│   ├── middleware/
│   │   └── auth.js          // Middleware для авторизації
│   └── routes/
│       ├── auth.js          // Реєстрація, логін, профіль
│       ├── favorites.js     // Улюблені оголошення
│       └── listings.js      // CRUD для оголошень, фільтрація
│
├── database/
│   └── schema.sql           // SQL-структура бази даних
│
├── frontend/
│   ├── index.html           // Головна сторінка
│   ├── script.js            // Логіка взаємодії з API
│   └── style.css            // Стилизація
│
└── TESTING_GUIDE.md         // Інструкції з тестування
```
--- 

## API Документація

- Swagger доступний за адресою: `http://localhost:3000/api-docs`
- Всі маршрути описані у Swagger (`backend/swagger.js`).
- Всі ендпоінти:
Listings — Операції з оголошеннями про житло:
- `GET /api/listings` — отримати всі оголошення
- `POST /api/listings` — додати нове оголошення
- `GET /api/listings/:id` — отримати одне оголошення за ID
- `PUT /api/listings/:id` — оновити оголошення за ID
- `DELETE /api/listings/:id` — видалити оголошення за ID
- `GET /api/listings/filter` — фільтрувати оголошення

Auth — Реєстрація користувачів і вхід:
- `/api/auth/register` — реєстрація нового користувача
- `/api/auth/login` — вхід користувача
- `POST /api/auth/logout` — вихід з акаунту
- `GET /api/auth/me` — отримати поточного користувача

Favorites — Керування обраними оголошеннями:
- `GET /api/favorites` — отримати список обраних оголошень користувача
- `POST /api/favorites/:listingId` — додати оголошення в обране
- `DELETE /api/favorites/:listingId` — видалити оголошення з обраних

---

## Авторизація та автентифікація

- Реалізовано через JWT (JSON Web Token).
- Після логіну токен зберігається у cookie.
- Middleware auth.js перевіряє токен для захищених маршрутів (додавання, редагування, видалення оголошень, улюблені).
- Кожен користувач має унікальний student_id (user_<id>).

## Особливості

- Фільтрація оголошень за ціною, районом, факультетом, статтю.
- CRUD для оголошень.
- Захист маршрутів через JWT.
- Swagger-документація.
- Зручна структура для розширення функціоналу.

---

## База даних

- MySQL, структура у `database/schema.sql`.
- Основні таблиці:
  - `users` — користувачі
  - `listings` — оголошення
  - `favorites` — улюблені оголошення
- Зв’язки: оголошення належать користувачу, улюблені — через зв’язок user-listing.

---
