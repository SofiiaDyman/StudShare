# 📚 Інструкція з тестування StudShare

## 🔍 Проблема: Чому не відображаються оголошення?

### Можливі причини:
1. **Backend сервер не запущений** - frontend не може підключитися до API
2. **База даних не налаштована** - немає даних для відображення
3. **CORS помилки** - браузер блокує запити між різними джерелами
4. **Помилки в консолі браузера** - перевір F12 → Console

---

## 🚀 Крок 1: Підготовка бази даних

### 1.1. Створи базу даних
```bash
# Підключись до MySQL
mysql -u root -p

# Виконай SQL скрипт
source database/schema.sql
```

Або вручну:
```sql
mysql -u root -p < database/schema.sql
```

### 1.2. Перевір, що дані є в базі
```sql
USE student_housing;
SELECT COUNT(*) FROM listings;
-- Має бути 10 записів
```

---

## 🔧 Крок 2: Налаштування Backend

### 2.1. Створи файл `.env` в папці `backend/`
```bash
cd backend
copy .env.example .env
```

### 2.2. Відредагуй `.env` файл:
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=твій_пароль_mysql
DB_NAME=student_housing
PORT=3000
JWT_SECRET=твій_випадковий_секретний_ключ
CLIENT_ORIGIN=http://localhost:5173
```

### 2.3. Встанови залежності (якщо ще не встановлено)
```bash
cd backend
npm install
```

---

## 🧪 Крок 3: Тестування через Swagger

### 3.1. Запусти backend сервер
```bash
cd backend
npm start
# або для розробки з автоперезавантаженням:
npm run dev
```

Маєш побачити:
```
✅ Успішно підключено до MySQL!
═══════════════════════════════════════
🚀 Server started successfully!
🌐 URL: http://localhost:3000
📚 API Documentation: http://localhost:3000/api-docs
```

### 3.2. Відкрий Swagger UI
1. Відкрий браузер
2. Перейди на: **http://localhost:3000/api-docs**
3. Ти побачиш інтерактивну документацію API

### 3.3. Протестуй endpoints через Swagger

#### ✅ Тест 1: Отримати всі оголошення
1. Знайди секцію **"Listings"**
2. Розгорни **GET /api/listings**
3. Натисни **"Try it out"**
4. Натисни **"Execute"**
5. Перевір відповідь - має бути масив з 10 оголошень

#### ✅ Тест 2: Створити нове оголошення
1. Розгорни **POST /api/listings**
2. Натисни **"Try it out"**
3. Встав приклад JSON:
```json
{
  "student_id": "test_student_123",
  "gender": "хлопець",
  "faculty": "Прикладної математики та інформатики",
  "course": 2,
  "specialty": "Комп'ютерні науки",
  "district": "Франківський",
  "address": "вул. Тестова, 1",
  "rooms_count": 2,
  "people_count": 1,
  "price": 2500,
  "utilities_included": true,
  "additional_info": "Тестове оголошення",
  "contact_phone": "+380671234567",
  "contact_telegram": "@test_user",
  "contact_instagram": "@test_user"
}
```
4. Натисни **"Execute"**
5. Перевір відповідь - має бути `201 Created`

#### ✅ Тест 3: Реєстрація користувача
1. Знайди секцію **"Auth"**
2. Розгорни **POST /api/auth/register**
3. Натисни **"Try it out"**
4. Встав JSON:
```json
{
  "full_name": "Тест Користувач",
  "email": "test@example.com",
  "password": "password123"
}
```
5. Натисни **"Execute"**

#### ✅ Тест 4: Вхід користувача
1. Розгорни **POST /api/auth/login**
2. Використай ті самі дані, що й при реєстрації
3. Перевір, що повертається `user` об'єкт

---

## 🌐 Крок 4: Підключення Frontend до Backend

### 4.1. Варіант A: Використання Live Server (рекомендовано)

#### Встанови Live Server розширення для VS Code:
1. Відкрий VS Code
2. Перейди до Extensions (Ctrl+Shift+X)
3. Знайди "Live Server" (автор: Ritwick Dey)
4. Встанови

#### Запусти frontend:
1. Відкрий файл `frontend/index.html`
2. Клікни правою кнопкою → **"Open with Live Server"**
3. Браузер відкриється автоматично (зазвичай на `http://127.0.0.1:5500`)

### 4.2. Варіант B: Використання Python HTTP сервера
```bash
cd frontend
python -m http.server 8000
# або для Python 3:
python3 -m http.server 8000
```
Потім відкрий: **http://localhost:8000**

### 4.3. Варіант C: Використання Node.js http-server
```bash
# Встанови глобально:
npm install -g http-server

# Запусти:
cd frontend
http-server -p 8000
```

---

## ✅ Крок 5: Перевірка роботи

### 5.1. Перевір консоль браузера
1. Відкрий DevTools (F12)
2. Перейди на вкладку **Console**
3. Маєш побачити:
   - `📱 Твій ID студента: student_...`
   - `✅ Завантажено 10 оголошень з API` (або більше, якщо додав нові)

### 5.2. Перевір вкладку Network
1. Відкрий DevTools (F12)
2. Перейди на вкладку **Network**
3. Онови сторінку (F5)
4. Знайди запит до `listings`
5. Перевір:
   - **Status**: має бути `200 OK`
   - **Response**: має містити масив оголошень

### 5.3. Якщо є помилки CORS:
- Переконайся, що backend запущений
- Перевір, що в `.env` правильно налаштований `CLIENT_ORIGIN`
- Перезапусти backend після зміни `.env`

---

## 🐛 Вирішення проблем

### Проблема: "Failed to fetch" в консолі
**Рішення:**
- Перевір, чи запущений backend (`http://localhost:3000`)
- Перевір, чи правильно налаштована база даних
- Перевір файл `.env` в папці `backend/`

### Проблема: "CORS policy" помилка
**Рішення:**
- Переконайся, що frontend відкривається через HTTP (не `file://`)
- Використовуй Live Server або інший локальний сервер
- Перевір налаштування CORS в `backend/server.js`

### Проблема: "Database connection error"
**Рішення:**
- Перевір, чи запущений MySQL
- Перевір дані підключення в `.env`
- Перевір, чи існує база даних `student_housing`

### Проблема: Оголошення не відображаються
**Рішення:**
1. Відкрий консоль браузера (F12)
2. Перевір, чи є помилки
3. Перевір Network tab - чи є запит до `/api/listings`
4. Перевір Response - чи повертаються дані
5. Перевір, чи в базі даних є записи:
   ```sql
   SELECT COUNT(*) FROM listings;
   ```

---

## 📝 Чеклист для тестування

- [ ] База даних створена і заповнена тестовими даними
- [ ] Файл `.env` створений і налаштований
- [ ] Backend сервер запущений без помилок
- [ ] Swagger UI доступний на `http://localhost:3000/api-docs`
- [ ] GET /api/listings повертає список оголошень
- [ ] Frontend відкривається через HTTP сервер (не file://)
- [ ] В консолі браузера немає помилок
- [ ] Оголошення відображаються на сторінці
- [ ] Можна додати нове оголошення
- [ ] Можна зареєструватися і увійти

---

## 🎯 Швидкий старт (TL;DR)

```bash
# 1. Створи базу даних
mysql -u root -p < database/schema.sql

# 2. Налаштуй backend
cd backend
copy .env.example .env
# Відредагуй .env файл
npm install
npm start

# 3. В іншому терміналі, запусти frontend через Live Server
# або через Python:
cd frontend
python -m http.server 8000

# 4. Відкрий http://localhost:8000 в браузері
```

---

**Успіхів у тестуванні! 🚀**

